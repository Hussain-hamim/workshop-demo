# Live AI: OpenAI is tried first, then OpenRouter, then OpenRouter fallback models.
# Leave all blank to run Demo mode (seeded Northline data, no network).

OPENAI_API_KEY=

OPENROUTER_API_KEY=
OPENROUTER_MODEL=
OPENROUTER_FALLBACK_MODELS=
